import React from "react";
import CropForm from "../components/CropForm";
import Layout from "../components/Layout";

const Crop = () => {
    return (
        <Layout>
            <CropForm />
        </Layout>
    );
};

export default Crop;
